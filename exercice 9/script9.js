var juste_prix = prompt ("Jeu: le juste prix");
var essai = 0;
var min = Math.min(20);
var max = Math.max(80);

function aleatoire () {
    for (var i = 0; i<(x,y); i++);
    x = (Math.floor(Math.random()*Math.min));
    y = (Math.floor(Math.random()*Math.max));
}
